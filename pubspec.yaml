name: chatia
description: "Aplicación ChatIA para interactuar con modelos de lenguaje de IA."
publish_to: 'none'

version: 0.0.2+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_localizations:
    sdk: flutter
  bloc: ^9.0.0
  flutter_bloc: ^9.0.0
  equatable: ^2.0.5
  get_it: ^8.0.3
  dartz: ^0.10.1
  google_fonts: ^6.2.1
  injectable: ^2.1.1
  mime_type: ^1.0.0
  http: ^1.2.2
  retry: ^3.1.0
  intl: ^0.20.2
  animate_do: ^4.2.0
  auto_route: ^10.1.2
  flutter_secure_storage: ^9.2.2
  flutter_dotenv: ^6.0.0
  uuid: ^4.5.1
  path_provider: ^2.1.5
  timeago: ^3.7.1
  modal_bottom_sheet: ^3.0.0
  url_launcher: ^6.3.2
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  auto_route_generator: ^10.2.4
  build_runner:

flutter_launcher_icons:
  android: true
  ios: true
  adaptive_icon_foreground: "assets/icons/chatia.png"
  image_path_ios: "assets/icons/chatia.png"
  image_path_android: "assets/icons/chatia.png"
  remove_alpha_ios: true

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/icons/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
flutter_intl:
  enabled: true
